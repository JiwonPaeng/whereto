@AGENTS.md

# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트

**어디갈래 / whereto** — 수험생이 (대학, 학과) 조합 두 개를 1:1 비교 투표하고, 누적 결과로 "수험생 선호도 지수"를 만드는 커뮤니티.

한글명 **어디갈래**, 영문·코드 슬러그 **`whereto`**. 이름을 새로 지을 일이 있으면 §17의 금지 규칙을 먼저 볼 것 — `rank`/`tier` 계열과 `어디가` 충돌은 이미 걸러낸 지뢰다.

**[docs/PROJECT_SPEC.md](docs/PROJECT_SPEC.md) 가 유일한 기준 문서다.** 현재 사실만 담은 **살아있는 명세**이며, 기능 작업 전 해당 절을 읽을 것.

[docs/DECISIONS.md](docs/DECISIONS.md) 는 **변경 이력**이다. "왜 그렇게 바뀌었는지"와 기각한 대안이 필요할 때만 본다. **명세와 다르면 명세가 맞다.**

**결정을 바꾸면 명세를 고치고 이력에도 남긴다. 둘 중 하나만 하면 안 된다.** 이 규칙을 어기면 명세가 다시 낡아 읽는 사람을 오도한다 — 실제로 한 번 그렇게 됐다.

**아래는 매 세션 상기가 필요한 것만 추린 것이다. 세부는 명세가 원본이며, 중복을 늘리지 말 것.**

**현재 진행 단계는 명세 §15에서 확인할 것.** 여기에 적으면 반드시 낡는다.

## 불변 원칙 (명세 §0 — 위반 금지)

1. **`votes` 테이블은 append-only.** DELETE 금지. UPDATE는 `is_valid` · `reason_public` · `reason`(NULL→값 1회) · 반응 카운터만. 트리거가 강제한다. (§5.6)
2. **ELO 계산은 Postgres RPC 함수 내부에서만.** 클라이언트가 점수를 계산해 전송하는 구조 금지. (§9.3, §13.3)
3. **매치업 쌍은 서버가 생성한다.** 클라이언트는 서버 발급 토큰 + 선택 결과만 제출한다. 임의 쌍 제출 불가. (§8.2)
4. **알고리즘 임계값·가중치를 코드에 하드코딩하지 않는다.** 전부 `app_config` 테이블에서 런타임 조회. (§5.5에 키 목록)
5. **"누가 어느 쪽을 골랐는지"의 연결은 타 유저에게 노출되지 않는다.** 이유 텍스트는 노출되나 익명 이유는 `profile_id`·`nickname`을 내보내지 않는다. RLS + 노출 컬럼을 고정한 뷰로 강제한다. (§4.1.2, §9.6)

## 작업 규칙

- 기능마다 우선순위 태그가 있다: `P0`(1차 릴리즈) / `P1`(2차) / `P2`(이후). **§15 로드맵 순서를 따르고 마일스톤을 건너뛰지 않는다.** M1까지가 1차 릴리즈다.
- §5(알고리즘) · §7(평판) · §8(어뷰징 방지) · §9(데이터 모델)은 이 서비스의 핵심 자산이다. **임의로 단순화하지 말고, 변경이 필요하면 먼저 질문한다.**
- **§16 미결정 사항은 임의로 정하지 않는다.** 목록은 명세 §16에 있고 해소될 때마다 갱신된다 — 여기 복사하지 말 것. 부딪히면 확인한다.
- 투표 질문 문구 — "당신이 이 두 곳을 모두 갈 수 있다면, 어디를 선택하시겠습니까?" — 는 서비스 정체성이다. UI 카피·공유 이미지·약관 어디에서도 "어디가 더 좋은 학교인가"로 바꾸지 않는다. (§1.2)
- 대학·학과 마스터 데이터는 **전면 수동 큐레이션**이다. 크롤링·API 자동 수집 코드를 작성하지 않는다. (§11.2)

## 함정 — 모르면 반드시 밟는 것들

여기 없는 구조·수치·스케줄은 **명세가 원본이다.** 이 절에 복사해 오지 말 것. 실제로 배치 시각을 여기 적어뒀다가 명세만 고쳐서 하루 만에 거짓이 된 적이 있다.

⚠️ **`majors`(표준 학과) 테이블은 없다. 되살리지 말 것.** 명세 구버전과 CLAUDE.md 구버전이 "majors ↔ display_name 분리를 반드시 유지"라고 했으나 D-007로 제거했다. 분류는 **계열 3개(인문/자연/예체능)까지만** 하고, 학과는 실제 명칭으로만 다룬다. (§9.1)

⚠️ **`w_age`가 로그인 유저 전원 1.0인 것은 버그가 아니다.** `app_config`의 `weight.age_enabled = 0`으로 §5.3 적용을 보류 중이다. 켜려면 값을 1로 바꾸면 되고 코드 수정은 필요 없다. (§5.3)

⚠️ **`layout.tsx`에서 쿠키를 읽지 말 것.** 읽는 순간 모든 페이지가 동적 렌더링이 되어 §13.3의 ISR이 통째로 무력화된다. 전역 헤더가 로그인 상태 없이 동작하는 이유다. ISR 페이지는 `createPublicClient()`를 쓴다.

⚠️ **`pg_cron`은 UTC로 돈다.** 한국 시각으로 착각하면 배치가 9시간 어긋난다. (§13.3)

⚠️ **Supabase `db-max-rows` = 1000. `limit`·`Range`로 못 넘긴다.** 1000행을 넘을 수 있는 조회는 `.range()`로 페이지네이션할 것. 안 하면 **조용히 잘린다** — 실제로 배치표가 1,763개 중 1,000개만 그리고 있었다. (§13.3)

⚠️ **서버측 Supabase 호출은 왕복 비용이 있다.** Vercel 리전은 `vercel.json`에서 `icn1`로 고정돼 있고(기본값 `iad1`이면 태평양을 건넌다), `proxy.ts`는 인증 쿠키가 없으면 즉시 반환한다. 같은 페이지에서 여러 쿼리를 던질 때는 `Promise.all`로 묶을 것. (§13.3)

⚠️ **RLS로 컬럼을 가릴 수 없다.** 타 유저에게 일부 컬럼만 보여야 하는 곳은 전부 노출 컬럼을 고정한 **뷰**로 처리한다. `public_*` 뷰를 수정할 때는 무엇이 새어나가는지 컬럼 단위로 확인할 것. (§9.6)

⚠️ **Program은 표가 있으면 삭제되지 않는다.** `votes`가 `on delete restrict`로 참조한다. 지우는 게 맞는 상황이라도 `is_active = false`가 정답이다 — 삭제하면 그 표가 만든 지수 이력이 끊긴다. 어드민 "제거" 버튼의 기본 동작도 비활성화다. (§8.5)

⚠️ **어드민 권한은 `profiles.is_admin`이고 부여는 service_role로만 한다.** 컬럼 GRANT가 이용자 UPDATE를 `nickname`·`status`·`track`로 묶어놨기 때문에 성립하는 구조다. **profiles의 UPDATE grant에 컬럼을 추가할 때 이걸 깨뜨리지 말 것.** 화면 가드(`/admin/layout.tsx`)는 권한의 근거가 아니다 — 근거는 `is_admin()`을 확인하는 RPC다. (§8.5)

⚠️ **`vote_submit`을 부를 때 `p_anon_id`를 빠뜨리면 조용히 틀린다.** 비로그인 상태에서 이 값이 없으면 `weight_applied = 0`으로 기록된다 — 표는 남는데 지수는 안 움직이고 중복 검사도 건너뛴다. 에러도 없고 화면도 정상이다. 투표 진입점을 새로 만들 때마다 확인할 것. (§10.6, D-016)

⚠️ **`votes`에 INSERT 정책이 없는 것은 실수가 아니다.** 직접 INSERT를 열면 클라이언트가 ELO 값을 지정할 수 있다. 기록은 `vote_submit` RPC로만 이루어진다. (§8.2)

**한 대학에 같은 계열 Program이 여럿인 게 정상이다** — 컴퓨터학과와 인공지능학과가 공존한다. Program의 정체성은 실제 학과명이다.

**어뷰징 방지는 부가 기능이 아니라 핵심 기능이다.** 지표가 조작되면 서비스 존재 이유가 사라진다. 방어선을 애플리케이션 레이어에만 두지 않는다. (§8)

**모바일/데스크톱 레이아웃을 각각 설계한다.** 한쪽을 늘리거나 줄여 재사용하지 않는다. `lg`(1024px)가 전환점. (§14.2)

## 스택 함정

버전과 채택 현황은 명세 §13.1이 원본이다. 여기는 **기억과 다르게 동작하는 것**만 적는다.

⚠️ **Next 16 / Tailwind v4는 학습 데이터와 다르다.** 추측하지 말고 `node_modules/next/`의 실제 타입 정의나 소스를 확인할 것. 루트 `AGENTS.md`가 `node_modules/next/dist/docs/` 참조를 지시하지만 **그 디렉터리는 비어 있다.**

⚠️ **Next 16에서 `middleware.ts`는 deprecated이고 `proxy.ts`가 대체한다.** 두 파일이 동시에 있으면 빌드가 실패한다. 시그니처는 `export default async function proxy(request: NextRequest)`.

⚠️ **Tailwind v4는 `tailwind.config.ts`가 없다.** 디자인 토큰은 [src/app/globals.css](src/app/globals.css)의 `@theme` 블록이다. 그리고 **사용되지 않은 `@theme` 변수를 출력에서 제거한다** — 유틸리티로 쓰면 괜찮지만 `var(--color-navy-500)`로 직접 참조하면 값이 조용히 빈다. 팔레트는 `@theme static`으로 전량 출력을 강제해 뒀으니 이 키워드를 지우지 말 것.

⚠️ **ESLint(React 19)가 effect 안의 setState와 렌더 중 `Date.now()`를 막는다.** 우회하려 하지 말고 데이터를 서버에서 넘기거나 ref로 옮기는 쪽으로 설계할 것.

**`AGENTS.md`는 삭제하지 말 것.** Next.js가 자동 생성하며, AGENTS.md가 관리 블록을 갖고 있는 동안에만 `next dev`가 CLAUDE.md를 건드리지 않는다. 지우면 CLAUDE.md가 덮어써진다.

## 명령어

```bash
npm run dev      # 개발 서버
npm run build    # 프로덕션 빌드
npm run start    # 빌드 결과 실행
npm run lint     # ESLint
```

DB 마이그레이션은 현재 Supabase MCP(`apply_migration`)로 원격 프로젝트에 직접 적용한다. **적용한 SQL은 반드시 `supabase/migrations/` 에 같은 내용으로 파일로 남겨 버전 관리한다** — MCP만 쓰면 스키마 이력이 저장소에 남지 않는다. 시드 데이터(마스터 데이터, `app_config`)도 마이그레이션 파일로 관리한다 (§11.2).

## 환경

Windows + VS Code. node 24.14.1 / npm 11.x. Supabase MCP 연결됨(조직 `Paengclub`), Vercel MCP 미연결.
